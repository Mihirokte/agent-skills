# Contributing

1. Node 22+
2. `npm install && npm run build`
3. `node dist/cli.js status`

Keep the capability matrix honest — do not claim MCP/hooks for targets that lack a stable native path.

Never commit secrets or machine inventory dumps.
