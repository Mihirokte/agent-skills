/** Cursor adapter — paths and behavior live in common.ts via capabilities. */
export {
  importMcp,
  importSkills,
  pushMcp,
  pushPortableHooks,
  pushSkills,
  readTargetMcp,
} from "./common.js";
