# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Structure

This is a personal workspace directory. Active projects live at the top level; archived/unused projects are in `old/`.

## Permissions

Python and pip commands are pre-approved (see `.claude/settings.local.json`).

## External Integrations

Gemini is configured with Figma MCP and Blender MCP servers (see `.gemini/settings.json`).
