{
  "mcpServers": {
    "example-echo": {
      "command": "npx",
      "args": ["-y", "mcp-server-everything"],
      "env": {
        "EXAMPLE_API_KEY": "${EXAMPLE_ECHO_API_KEY}"
      }
    }
  }
}
