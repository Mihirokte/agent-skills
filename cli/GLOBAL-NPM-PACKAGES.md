# Global npm packages (AI-related context)

Output of `npm list -g --depth=0` on this machine at inventory time:

| Package | Version | Notes |
|---------|---------|--------|
| `@google/gemini-cli` | 0.27.3 | Google Gemini CLI |
| `@linear/cli` | 0.0.5 | Linear CLI (complements Linear MCP in Claude Code project state) |
| `happy-coder` | 0.13.0 | `happy` / **`happy-mcp`** — client for Claude Code / Codex |
| `pnpm` | 10.28.2 | Package manager |

Global prefix: typically `C:\Users\rentk\AppData\Roaming\npm`.

Project-local `node_modules` and per-repo MCP definitions are **not** enumerated here.
